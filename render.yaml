services:
  - type: web
    name: pedro-barros-api-otimizada
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT main:app
    envVars:
      - key: DEBUG
        value: false
      - key: FLASK_ENV
        value: production

